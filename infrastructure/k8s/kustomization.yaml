apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

resources:
  - secrets.yaml
  - postgres.yaml
  - spring-boot.yaml

configMapGenerator:
  - name: rdv-db-init
    files:
      - sql/v1_schema.sql
      - sql/v2_table.sql
      - sql/v3_services.sql
